---
- name: docker
  hosts: all
  gather_facts: false
  tasks:

    - name: Update apt cache
      apt: update_cache=yes cache_valid_time=3600
      become: yes

    - name: Install dependencies
      apt:
        pkg:
        - apt-transport-https
        - ca-certificates
        - curl
        - software-properties-common
        - gnupg-agent
        state: latest
      become: yes
      
    - name: Add Docker’s official GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present
      become: yes

    - name: Add apt repository
      apt_repository:
        repo: deb [arch=amd64] https://download.docker.com/linux/ubuntu focal stable
        state: present
      become: yes

    - name: Install Docker
      apt:
        pkg:
        - docker-ce
        - docker-ce-cli 
        - containerd.io
        state: present
      become: yes

    - name: Add user to docker group
      user:
        name: "ubuntu"
        group: docker
      become: yes

    - name: run docker
      command: docker run -d --name lesson2 --rm -p 80:80 lymych/itea_exam:latest